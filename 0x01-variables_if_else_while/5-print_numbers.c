#include <stdio.h>
/**
 * main - main uses for loop to loop through a given set
 * of object(arg)
 * Description: print all numbers of base 10 which contains one digit
 * Return: 0
*/
int main(void)
{
	int i;
	for (i = 0; i < 10; i++)
	{
		printf("%i", i)
	}
	putchar('\n');

	return (0);
}
